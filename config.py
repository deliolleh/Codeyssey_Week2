"""게임 규칙을 한곳에 모아 둔 설정 파일.

밸런스를 바꾸고 싶을 때 이 파일만 열면 되도록 흩어져 있던 값을 모았다.
여러 파일에 숫자가 박혀 있으면 "힌트를 2개로 늘리자" 같은 간단한 변경도
어디를 고쳐야 하는지 찾는 일부터 시작해야 한다.

.env가 아니라 파이썬 파일로 둔 이유
    .env는 값이 전부 문자열이라 딕셔너리를 담으려면 직접 쪼개 변환해야 하고,
    파이썬이 자동으로 읽어주지도 않아 별도 라이브러리가 필요하다.
    또 .env는 보통 저장소에 올리지 않는 파일이라, 규칙이 빠진 채로 배포된다.
    여기 있는 값들은 비밀도 아니고 실행 환경마다 달라야 하는 것도 아니다.
    바뀌면 '다른 환경'이 아니라 '다른 게임'이 되므로 코드와 함께 관리한다.
"""

# ---------- 힌트 ----------

# 한 판을 시작할 때 들고 가는 힌트 개수
STARTING_HINTS = 1

# 몇 문제를 연속으로 맞힐 때마다 힌트를 1개씩 더 줄지
HINT_REWARD_STREAK = 5

# 힌트를 쓰면 그 문제의 점수를 몇 분의 1로 줄일지 (2면 절반, 내림)
# 1점짜리 문제는 0점이 되는데, 점수를 포기하고 연승을 잇는 선택이 되도록 둔 것이다.
HINT_POINT_DIVISOR = 2

# ---------- 점수 ----------

# 문제 유형별 기본 점수. 답을 떠올리기 어려운 유형일수록 높게 잡았다.
TYPE_BASE_POINT = {
    "ox": 1,  # 찍어도 절반은 맞는다
    "choice": 2,  # 선택지 안에 답이 있다
    "short": 3,  # 아무 단서 없이 직접 써야 한다
}

# 난이도 배수. 문제 점수 = 유형 기본 점수 x 난이도 배수
DIFFICULTY_WEIGHT = {
    "easy": 1,
    "normal": 2,
    "hard": 3,
}

# 화면에 보여줄 난이도 이름
DIFFICULTY_LABEL = {
    "easy": "쉬움",
    "normal": "보통",
    "hard": "어려움",
}

# 난이도 값이 잘못됐을 때 대신 쓸 값
DEFAULT_DIFFICULTY = "normal"

# ---------- 기록 ----------

# 한 사람이 남길 수 있는 게임 기록의 최대 개수.
# 제한이 없으면 게임을 할수록 state.json이 끝없이 커진다.
# 넘으면 오래된 기록부터 지운다.
MAX_HISTORY = 50

# 점수 확인 화면에서 한 번에 보여줄 최근 기록 수
RECENT_HISTORY_COUNT = 5

# 판이 끝난 방식을 화면에 보여줄 이름
ENDING_LABEL = {
    "cleared": "완주",
    "wrong": "오답",
    "gave_up": "중단",
}

# ---------- 문제 형식 ----------

# 선택형 문제의 선택지 개수 범위.
# 2개 미만은 문제가 성립하지 않고, 너무 많으면 터미널에서 한눈에 읽기 어렵다.
MIN_CHOICES = 2
MAX_CHOICES = 6

# 문제 문장이나 선택지로 받을 수 있는 최대 글자 수
MAX_TEXT_LENGTH = 200
